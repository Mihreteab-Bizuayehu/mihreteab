
import { Metadata } from "next";

export const metadata: Metadata = {
    title   : "Blog-MeraBizu",
}
export default async function Blog() {
    return (
        <div>
            <h1>Blog</h1>
        </div>
    );
}